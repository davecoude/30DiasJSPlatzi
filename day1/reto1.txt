📒En este desafío encontrarás una función
llamada solution que recibe un parámetro llamado valor.

Debes encontrar el tipo de dato del parámetro valor y
retornarlo desde la función solution.

Recuerda que el parámetro valor será distinto por cada distinta
forma en que ejecutemos la función solution.

Por ejemplo:

💻 Dados los siguientes llamados a la función solution:

    <code>
        solution(1)
        solution("Dieguillo")
        solution(true)
    </code>

✅ Debes obtener los siguientes resultados:

    <expected>
        "number"
        "string"
        "boolean"
    </expected>